name: 'Build Sofle Choc Pro'
on: workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["sofle_choc_pro_left","sofle_choc_pro_right"]'
      extra_shield: '["sharp_mip"]'
